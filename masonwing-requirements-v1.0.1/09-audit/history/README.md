# Lịch sử bất biến

Các file trong `v1.0.0/` được chép nguyên byte từ ZIP đầu vào. Tên sản phẩm cũ và đường dẫn môi trường trước là một phần của receipt lịch sử, không phải tên triển khai hoặc evidence mới. Không dùng kết quả này làm product acceptance.
